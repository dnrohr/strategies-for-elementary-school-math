# Task `WEB` — Book explorer

## Owned paths

- `site/**`
- `scripts/**`
- `.eleventy.js`
- `package.json`

Maintain a static, accessible, base-path-safe GitHub Pages build. Manuscript remains the source of truth. Preserve deep links, keyboard access, visible focus, reduced motion, and semantic HTML.

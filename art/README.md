# Art production

Use `raster/` for organic subjects, `vectors/` for exact mathematical structures, `composites/` for combined work, `prompts/` for reproducible briefs, and `rejected/` for failed generations plus rejection reasons. Never embed labels or equations in generated raster images.

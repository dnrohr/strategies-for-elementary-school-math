# Project setup UI evidence

- `book-explorer-desktop.png`: landing page at 1440 × 1100, captured from the final local production build.
- `book-explorer-narrow.png`: landing page at 500 × 932, demonstrating the single-column responsive layout.

The chapter filter was also exercised with `fractions` (three matching chapters), and chapter navigation was verified on the generated `3/4 of 20 = ?` page.

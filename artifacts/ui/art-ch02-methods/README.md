# ART-CH02-METHODS visual QA

Review date: 2026-09-07

- Inspected `desktop-method-01.png` through `desktop-method-12.png` at a 1440 px browser viewport after a clean site build.
- Inspected the two generated-hand composites (Methods 01 and 08) at native raster size, 1440 px placement, and 390 px placement. Each contains one plausible, consistently oriented hand and no raster text or countable mathematical objects.
- Replaced the initial Method 01 vector with a composite that follows the brief's staged action: a 3×5 start array, exactly 8 hatched positions moving to a tray, and 7 solid counters remaining.
- Replaced the initial Method 08 abstract shapes because they could be mistaken for eight fingers. The accepted composite uses one real hand with exactly eight circular vector tracking marks aligned to outcomes 14 through 7.
- Found and corrected a Method 08 result-label overflow before recapturing its desktop and narrow evidence.
- Confirmed all twelve method figures load with non-empty alt text. At 390 px every figure stays within the 390 px document width; measurements are recorded in `browser-metrics.json`.
- Confirmed subtraction direction, exact interval counts, staged removals, compensation signs, bar partitions, ten-frame quantities, inverse relation, and regrouping labels in the rendered figures.

The screenshots are evidence of the current implementation, not source assets.
